# Mod 升级指南（内部文档）

本文件记录了相对于上游 `SQmusic` 原版所做的全部改动，供日后跟进原版升级时快速重新迁移。

---

## 一、Mod 概述

- **上游项目**：`SQmusic`（simple_sq_music_plus v3.x 分支）
- **本 Mod 目标**：在不破坏原版任何逻辑的前提下，通过"钩子+开关"模式叠加新功能
- **原则**：所有新功能默认关闭（`false`），升级时用户需手动到"设置"页开启

---

## 二、新增文件（整包复制）

升级时这些文件直接复制到新版本，**无需修改**：

```
src/main/java/com/sqmusicplus/v3/addon/
├── config/
│   ├── AddonConfigCache.java       # 从 SqConfigCache 读取 addon.* 配置项
│   └── AddonConfigInitializer.java # 启动时向数据库写入 addon 配置项（幂等）
├── controller/
│   └── AddonPersonConfigController.java  # REST API: GET/POST /api/addon/config/...
├── enums/
│   └── AddonConfigEnum.java        # 枚举：所有配置项的 key 和默认值
└── rules/
    ├── AlbumArtistRule.java        # 功能：专辑艺术家归档
    ├── PathSanitizeRule.java       # 功能：路径非法字符替换
    └── TrackNumberRule.java        # 功能：写入音轨序号到 Metadata
```

### AddonConfigEnum — 配置项一览

| Key | 类型 | 默认 | 说明 |
|-----|------|------|------|
| `addon.path.slash.replace.enable` | boolean | false | 路径非法字符替换总开关 |
| `addon.path.slash.replace.char` | input | `-` | 替换后的字符（如 `+`） |
| `addon.album.artist.folder.enable` | boolean | false | 专辑按专辑艺术家归档 |
| `addon.track.number.enable` | boolean | false | 写入音轨序号到 tag |

---

## 三、修改文件 — `SearchHanderAbstract.java`

**路径**：`src/main/java/com/sqmusicplus/v3/plug/base/hander/SearchHanderAbstract.java`

### 3.1 新增 import（加在已有 import 块末尾）

```java
import com.sqmusicplus.v3.addon.rules.AlbumArtistRule;
import com.sqmusicplus.v3.addon.rules.PathSanitizeRule;
import com.sqmusicplus.v3.addon.rules.TrackNumberRule;
```

### 3.2 Hook 1 — 专辑艺术家归档

**位置**：`dnonloadAndSaveToFile` 方法内，在 `baseMusicMainArtistName_temp` 初始赋值后、进入 `try { String open_symbol_remove ...}` 之前。

**原版代码片段**：
```java
String baseMusicMainArtistName_temp =  "群星";
if (StringUtils.isNotBlank( music.getMusicArtists().get(0).trim())){
    baseMusicMainArtistName_temp =  music.getMusicArtists().get(0).trim();
}

// ← 在此处插入以下一行 ↓
try {
    String open_symbol_remove = SqConfigCache.getSqConfigValue(...
```

**插入代码**：
```java
baseMusicMainArtistName_temp = AlbumArtistRule.apply(baseMusicMainArtistName_temp, music, searchHander);
```

**作用**：如果专辑有合唱艺术家（albumArtist），用专辑艺术家替换第一位歌手作为目录名，解决合唱专辑分散存储问题。

---

### 3.3 Hook 2 — 路径非法字符替换

**位置**：`dnonloadAndSaveToFile` 方法内，原版 `open_symbol_remove` 的 try-catch 块结束后，`final String baseMusicName = baseMusicName_temp` 之前。

**原版代码片段**：
```java
            } catch (Exception e) {
               log.error("歌曲信息移除特殊字符失败",e);
            }
            // ← 在此处插入以下 4 行 ↓
            final String baseMusicName = baseMusicName_temp;;
```

**插入代码**：
```java
                baseMusicName_temp = PathSanitizeRule.apply(baseMusicName_temp);
                baseMusicArtistName_temp = PathSanitizeRule.apply(baseMusicArtistName_temp);
                baseMusicAlbumName_temp = PathSanitizeRule.apply(baseMusicAlbumName_temp);
                baseMusicMainArtistName_temp = PathSanitizeRule.apply(baseMusicMainArtistName_temp);
```

**作用**：将路径中的 `/ \ : * ? " < > |` 替换为用户配置的字符（默认 `-`），解决 Windows/NAS 文件名非法字符问题。

---

### 3.4 Hook 3 — 写入音轨序号到 Metadata

**位置**：`extracted` 方法（或 `onComplete` lambda）内，写入 tag 标签处。

找到如下原版调用：
```java
// 原版（无 trackNumber 参数，无 mainArtistForTag 变量）：
MusicUtils.setMediaFileInfo(onSuccess, music.getMusicName(), music.getMusicAlbum(),
    String.join(";", music.getMusicArtists()), "SqMusic", music.getMusicLyric(),
    albumfile, mainArtistForTag);
```

**替换为**：
```java
Integer trackNumber = TrackNumberRule.resolve(music, downloadInfo);
String mainArtistForTag = (albumArtistName != null && !albumArtistName.isEmpty()) ? albumArtistName : music.getMusicArtists().get(0);
MusicUtils.setMediaFileInfo(onSuccess, music.getMusicName(), music.getMusicAlbum(),
    String.join(";", music.getMusicArtists()), "SqMusic", music.getMusicLyric(),
    albumfile, mainArtistForTag, trackNumber);
```

> ⚠️ 注意：`albumArtistName` 是 `extracted` 方法的参数，如果原版签名有改动需同步确认。

---

## 四、修改文件 — `MusicUtils.java`

**路径**：`src/main/java/com/sqmusicplus/v3/utils/MusicUtils.java`

### 4.1 重载：为 `setMediaFileInfo` 增加 `trackNumber` 参数

**原版方法签名**（带 `mainArtist` 的那个）：
```java
public static synchronized MultimediaInfo setMediaFileInfo(File file, String title, String album,
    String artists, String comment, String lyrics, File image, String mainArtist)
```

**操作**：
1. 将上述方法**重命名**，末尾新增 `Integer trackNumber` 参数，在方法体内写入 TRACK tag（见下）
2. 保留原签名作为**向后兼容重载**，内部转发到新方法，trackNumber 传 null

**新方法签名**：
```java
public static synchronized MultimediaInfo setMediaFileInfo(File file, String title, String album,
    String artists, String comment, String lyrics, File image, String mainArtist, Integer trackNumber)
```

**在 tag 写入区域增加如下代码**（在 `tag.setField(FieldKey.ALBUM_ARTIST, ...)` 之后）：
```java
if (trackNumber != null && trackNumber > 0) {
    tag.setField(FieldKey.TRACK, String.valueOf(trackNumber));
}
```

**向后兼容重载**（原方法签名，转发）：
```java
public static synchronized MultimediaInfo setMediaFileInfo(File file, String title, String album,
    String artists, String comment, String lyrics, File image, String mainArtist)
        throws ... {
    return setMediaFileInfo(file, title, album, artists, comment, lyrics, image, mainArtist, null);
}
```

---

## 五、新增部署文件

升级时整体复制，内容稳定，一般不需要改动：

| 文件 | 说明 |
|------|------|
| `docker-compose-ghcr.yml` | 第三方用户用的部署文件，含 JVM 内存限制 |
| `.github/workflows/build-and-release.yml` | CI：推送到 3.0 分支自动构建 amd64+arm64 镜像 |
| `Dockerfile` | 多架构两阶段构建（amazoncorretto:17） |
| `free_plug/` | 浏览器插件目录，用于免费用户 |
| `fnOS/` | 飞牛 OS 应用商店配置 |
| `script/` | 辅助脚本 |

### docker-compose-ghcr.yml 关键点

- `command: ["sh", "-c", "java -Xms256m -Xmx512m -jar app.jar"]` — 限制 JVM 堆内存
  - ⚠️ Dockerfile 的 CMD 是硬编码的，**JAVA_OPTS 环境变量对它无效**，必须用 `command:` 覆盖
- MySQL 容器必须加 `--character-set-server=utf8mb4` 防止中文乱码

---

## 六、数据库自动初始化

**无需手动执行 SQL**。`AddonConfigInitializer.java` 在每次服务启动时自动向 `sq_config` 表插入缺失的 addon 配置项（幂等，已存在则跳过）。

执行顺序由 `@Order(102)` 控制，在原版配置加载（`@Order(101)` 左右）之后运行。

---

## 七、升级操作步骤

1. **从上游拉取新代码**
2. **直接复制 `src/main/java/com/sqmusicplus/v3/addon/` 整个目录**到新版本对应位置
3. **对照第三、四节逐一检查** `SearchHanderAbstract.java` 和 `MusicUtils.java` 改动点：
   - 如果原版对这两个文件有修改，手动合并（diff 工具辅助）
   - 重点排查 `dnonloadAndSaveToFile` 方法的结构变化
4. **复制 `docker-compose-ghcr.yml` / `Dockerfile` / CI workflow**
5. **本地 `mvn package` 验证编译通过**
6. **推送到 3.0 分支触发 CI**，等待镜像构建成功

---

## 八、常见问题备忘

| 问题 | 原因 | 解决 |
|------|------|------|
| 设置页中文乱码 | SQL 执行时未指定 charset | 执行时加 `--default-character-set=utf8mb4` |
| 音轨序号不写入（网易云） | dataInfo 字段名为 `"no"`，原版 CANDIDATE_KEYS 缺失 | `TrackNumberRule.CANDIDATE_KEYS` 已包含 `"no"` |
| OOM / TimeoutException | 下载列表 SQL 无分页，全量加载到 JVM | docker-compose `command:` 加 `-Xmx512m` |
| JAVA_OPTS 不生效 | Dockerfile CMD 硬编码 | 必须用 `command:` 覆盖，不能用环境变量 |
| 合唱专辑目录分散 | 原版用第一位歌手作目录 | 开启 `addon.album.artist.folder.enable` |

---

*最后更新：2026-03-24，基于原版 v3.0.35*
